# AI 赋能的智能简历分析系统 — 需求规格说明

## 1. 项目背景

在企业招聘流程中，HR 需人工筛选大量简历，存在效率低、标准不统一、易遗漏关键信息等问题。为提升筛选效率、降低人力成本、实现客观量化评估，需构建一套后端服务，支持 PDF 简历自动解析、关键信息提取、AI 评分与岗位匹配，为招聘人员提供快速筛选能力。

## 2. 运行与技术约束

| 约束项   | 要求                                       |
| -------- | ------------------------------------------ |
| 运行环境 | 阿里云 Serverless（函数计算 FC）           |
| 开发语言 | Python                                     |
| API 风格 | RESTful                                    |
| 交付时限 | 收到题目后 24 小时内完成并提交             |
| 交付物   | 后端服务代码 + 可公开访问的前端页面（GitHub Pages 或等效服务） |

## 3. 功能需求（必选模块）

### 模块一：简历上传与解析

- 提供 RESTful 接口，支持单个 PDF 文件上传
- 解析 PDF 内容，支持多页简历文本完整提取
- 文本清洗与结构化：
  - 去除冗余字符、乱码、多余空行
  - 合理分段，保留原文逻辑结构
  - 输出干净、可用于后续 NLP 处理的纯文本
- 接口输入：PDF 文件（`multipart/form-data`）
- 接口输出：清洗后的文本及基本文件信息（页数、文件名等）

### 模块二：关键信息提取（AI）

基于提取的简历文本，调用 AI 模型自动抽取结构化字段：

**（1）基本信息（必选）**

| 字段 | 说明 |
| ---- | ---- |
| 姓名 |      |
| 电话 |      |
| 邮箱 |      |
| 地址 |      |

**（2）求职信息（加分项）**

| 字段     | 说明 |
| -------- | ---- |
| 求职意向 |      |
| 期望薪资 |      |

**（3）背景信息（加分项）**

| 字段     | 说明                       |
| -------- | -------------------------- |
| 工作年限 |                            |
| 学历背景 | 学历、学校、专业、毕业时间 |
| 项目经历 | 项目名称、职责、技术栈     |

- 输出为结构化 JSON，字段缺失时明确标记为 `null` 或空字符串

### 模块三：简历评分与岗位匹配

- 提供 RESTful 接口，接收岗位需求描述（纯文本）
- 对岗位需求进行关键词提取与结构化分析（技能要求、经验要求、学历要求等）
- 简历与岗位匹配计算：
  - 技能匹配率
  - 工作经验相关性
  - 学历匹配度
  - 综合匹配度评分（0–100）

**加分项：**
- 利用 AI 大模型进行语义级匹配与精准评分
- 输出匹配理由、优势点、缺失关键词

### 模块四：结果返回与缓存

所有分析结果以 JSON 结构化返回，至少包含：

- 简历基本文件信息
- 清洗后文本
- 关键信息提取结果
- 岗位匹配度及分项得分
- 匹配摘要 / 建议（如有）

**加分项：**
- 引入缓存（如 Redis），对已解析 / 已评分简历按文件指纹或 ID 缓存结果
- 避免重复解析与重复 AI 调用，提升响应速度、降低成本

### 模块五：前端页面（必选）

- 技术栈（Vue + JS）
- 核心功能：
  - 简历 PDF 上传
  - 解析结果展示（文本、结构化信息）
  - 岗位需求输入
  - 匹配评分结果可视化展示
- 部署要求：
  - 必须部署至 GitHub Pages 或其他可公开访问的云服务
  - 保证评审可直接线上访问验收

## 4. 非功能需求

### 性能

| 指标       | 要求                                          |
| ---------- | --------------------------------------------- |
| 单份简历   | ≤ 10MB、≤ 10 页                               |
| 全流程耗时 | 解析 + AI 提取 + 匹配 ≤ 30s                   |
| 接口响应   | 正常情况下 ≤ 5s（不含 AI 深度推理）           |

### 稳定性

- 支持常见异常处理：非 PDF 文件、文件过大、损坏 PDF、AI 调用失败、网络超时
- 错误返回清晰错误码与提示信息

### 安全性

- 不持久化存储敏感个人信息（或加密存储）
- 接口具备基础参数校验，防止恶意上传

### 可维护性

- 代码结构清晰、命名规范、关键逻辑注释完整
- 提供 README：部署方式、环境变量、接口文档、依赖清单

## 5. 接口概述（RESTful）

### `POST /api/resume/upload`

| 项目 | 说明                     |
| ---- | ------------------------ |
| 功能 | 上传并解析 PDF 简历      |
| 输入 | `file`（PDF）            |
| 输出 | 清洗文本、文件信息、resumeId |

### `POST /api/resume/extract`

| 项目 | 说明                                                              |
| ---- | ----------------------------------------------------------------- |
| 功能 | AI 提取关键信息                                                   |
| 输入 | `resumeId` 或文本内容                                             |
| 输出 | 结构化字段（姓名、电话、邮箱、地址、求职意向、工作年限、学历、项目经历等） |

### `POST /api/match`

| 项目 | 说明                                                         |
| ---- | ------------------------------------------------------------ |
| 功能 | 简历与岗位匹配及评分                                         |
| 输入 | `resumeId` + `jobDescription`（文本）                        |
| 输出 | 匹配度总分、分项得分、匹配关键词、缺失关键词、AI 建议（如有） |

## 6. 验收标准

| 维度           | 权重 | 要求                                                       |
| -------------- | ---- | ---------------------------------------------------------- |
| 功能完整性     | 30%  | 五大必选模块全部实现并可正常运行；接口可调用、前端可访问、流程可通 |
| 代码质量       | 25%  | 结构清晰、命名规范、注释合理、无明显冗余                     |
| 工程化实践     | 20%  | Git 提交规范、README 完整、错误处理完善                     |
| 技术深度       | 15%  | AI 模型选型合理、调用方式规范；缓存 / 性能优化思路清晰       |
| 加分项         | 10%  | 求职信息/背景信息提取；AI 精准评分；Redis 缓存；前端交互体验良好 |
